// import createHashHistory from 'history/createBrowserHistory';
import { createBrowserHistory } from "history";
// require("history").createHashHistory();

export default createBrowserHistory()// createHashHistory();
